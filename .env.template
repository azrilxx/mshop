# mShop Configuration Template
# Copy this file to .env.local and fill in your values

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Service Configuration (Choose one)

# Option 1: Mailchimp (Recommended for production)
MAILCHIMP_API_KEY=your_mailchimp_api_key_here
MAILCHIMP_SERVER_PREFIX=us1
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=mShop Marketplace

# Option 2: SMTP Fallback (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password_here

# Email Templates and Branding
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=mShop Marketplace

# Session and Security
SESSION_SECRET=your_secure_session_secret_here

# Database (Replit DB is used by default)
# No additional configuration needed for Replit DB